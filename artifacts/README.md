# Runtime Artifacts

This directory intentionally starts without a model checkpoint. The earlier
v0.8 byte checkpoint uses a different input/output vocabulary and belongs to
the historical build.

Create a v1.1 Qwen-token reference checkpoint with the CPU training command in
the project README after providing the locked tokenizer revision.
